
import scala.annotation.tailrec
import scala.collection.mutable

object lab4 extends App {
//  def lt(a: Int, b: Int): Boolean = a < b
//
//  def sortListBubble[T](l: List[T], lt: (T, T) => Boolean): List[T] = {
//    def run[T](l: List[T], lt: (T, T) => Boolean): (List[T], Boolean) = {
//      l match {
//        case h1 :: h2 :: t => {
//          if (lt(h1, h2)) {
//            val (nl, kon) = run(h2 :: t, lt)
//            (h1 :: nl, kon)
//          } else {
//            val (nl, kon) = run(h1 :: t, lt)
//            (g :: nl, true)
//          }
//        }
//        case _ => (l, false)
//
//      }
//    }
//
//    val (newL, continue) = run(l, lt)
//    if (continue) sortListBubble(newL, lt)
//    else newL
//  }


}